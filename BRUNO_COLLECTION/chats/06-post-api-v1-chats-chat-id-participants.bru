meta {
  name: POST /api/v1/chats/{chat_id}/participants
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/v1/chats/{{chat_id}}/participants?user_to_add={{user_to_add}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}
