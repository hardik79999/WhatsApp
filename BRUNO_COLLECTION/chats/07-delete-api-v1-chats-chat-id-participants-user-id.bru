meta {
  name: DELETE /api/v1/chats/{chat_id}/participants/{user_id}
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/api/v1/chats/{{chat_id}}/participants/{{user_id}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}
