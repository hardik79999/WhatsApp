meta {
  name: POST /api/v1/chats/{chat_id}/participants/{user_id}/promote
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/v1/chats/{{chat_id}}/participants/{{user_id}}/promote
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}
